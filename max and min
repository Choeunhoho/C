#include <stdio.h>

int main()
{
    int A[10]={36,43,10,87,47,560,100,99,76,33,};
    int b=A[0];
    int c=A[0];
    for(int a = 1;a<10;a++)
    {
        if(A[a]>b)
        {
            b=A[a];
        }
        if(A[a]<c)
        {
            c=A[a];
        }
    }
    printf("최댓값은 : %d, 최솟값은 :%d",b,c);
    return 0;
}


/*
8/20
배열을 이용하여 배열에서의 최솟값과 최댓값을 구하는 코드를 만들었다.
만약 i가 1이면 b는 i보다작기 때문에 43이되고 c는 그대로 36이다.
그리고 i가 2일땐 b는 36,c는 10이다
i가 3일땐 b는 87,c는 10이다.
*/
